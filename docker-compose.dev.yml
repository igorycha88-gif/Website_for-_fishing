version: '3.8'

x-logging: &logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"
    labels: "service"

x-backend-resources: &backend-resources
  limits:
    cpus: '1'
    memory: 512M
  reservations:
    cpus: '0.25'
    memory: 256M

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-fishing_db}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres_password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
    ports:
      - "5432:5432"
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-fishing_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 128M
        reservations:
          cpus: '0.1'
          memory: 64M

  vault:
    image: hashicorp/vault:1.15
    ports:
      - "8200:8200"
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: ${VAULT_DEV_ROOT_TOKEN_ID:-dev-root-token}
      VAULT_ADDR: "http://0.0.0.0:8200"
    cap_add:
      - IPC_LOCK
    networks:
      - fishing-network
    volumes:
      - vault_data:/vault/data
      - ./vault/policies:/vault/policies:ro
      - ./vault/init.sh:/vault/init.sh:ro
    command: server -dev -dev-root-token-id=${VAULT_DEV_ROOT_TOKEN_ID:-dev-root-token}
    restart: unless-stopped
    logging: *logging
    healthcheck:
      test: ["CMD", "vault", "status"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 128M

  vault-init:
    image: hashicorp/vault:1.15
    networks:
      - fishing-network
    volumes:
      - ./vault/policies:/vault/policies:ro
      - ./vault/init.sh:/vault/init.sh:ro
    environment:
      VAULT_ADDR: "http://vault:8200"
      VAULT_TOKEN: ${VAULT_DEV_ROOT_TOKEN_ID:-dev-root-token}
      SECRET_KEY: ${SECRET_KEY:-your-secret-key-change-in-production-min-32-chars}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres_password}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:-}
      CLOUDINARY_API_SECRET: ${CLOUDINARY_API_SECRET:-}
      OPENWEATHERMAP_API_KEY: ${OPENWEATHERMAP_API_KEY:-}
      MAPBOX_API_KEY: ${MAPBOX_API_KEY:-}
    entrypoint: ["/bin/sh", "/vault/init.sh"]
    depends_on:
      vault:
        condition: service_healthy
    restart: "no"

  auth-service:
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://postgres:postgres_password@postgres:5432/fishing_db}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      SECRET_KEY: ${SECRET_KEY}
      ALGORITHM: ${ALGORITHM:-HS256}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-30}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS:-7}
      EMAIL_SERVICE_URL: http://email-service:8005
      EMAIL_SERVICE_API_KEY: ${EMAIL_SERVICE_API_KEY}
      ENABLE_EMAIL_SENDING: ${ENABLE_EMAIL_SENDING:-false}
      EMAIL_CODE_EXPIRE_MINUTES: ${EMAIL_CODE_EXPIRE_MINUTES:-15}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOGSTASH_URL: ${LOGSTASH_URL:-http://logstash:5000}
      SERVICE_NAME: ${SERVICE_NAME:-auth-service}
      USE_VAULT: ${USE_VAULT:-false}
      VAULT_ADDR: ${VAULT_ADDR:-http://vault:8200}
      VAULT_ROLE_ID: ${AUTH_VAULT_ROLE_ID:-}
      VAULT_SECRET_ID: ${AUTH_VAULT_SECRET_ID:-}
      ENVIRONMENT: ${ENVIRONMENT:-development}
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      email-service:
        condition: service_started
      vault:
        condition: service_healthy
    deploy:
      resources: *backend-resources

  email-service:
    build:
      context: ./services/email-service
      dockerfile: Dockerfile
    ports:
      - "8006:8005"
    environment:
      SMTP_HOST: ${SMTP_HOST:-smtp.yandex.ru}
      SMTP_PORT: ${SMTP_PORT:-465}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME:-FishMap}
      EMAIL_SERVICE_API_KEY: ${EMAIL_SERVICE_API_KEY}
      ENABLE_EMAIL_SENDING: ${ENABLE_EMAIL_SENDING:-false}
      EMAIL_CODE_EXPIRE_MINUTES: ${EMAIL_CODE_EXPIRE_MINUTES:-15}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOGSTASH_URL: ${LOGSTASH_URL:-http://logstash:5000}
      SERVICE_NAME: ${SERVICE_NAME:-email-service}
      USE_VAULT: ${USE_VAULT:-false}
      VAULT_ADDR: ${VAULT_ADDR:-http://vault:8200}
      VAULT_ROLE_ID: ${EMAIL_VAULT_ROLE_ID:-}
      VAULT_SECRET_ID: ${EMAIL_VAULT_SECRET_ID:-}
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      vault:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 128M

  places-service:
    build:
      context: ./services/places-service
      dockerfile: Dockerfile
    ports:
      - "8002:8001"
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://postgres:postgres_password@postgres:5432/fishing_db}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      SECRET_KEY: ${SECRET_KEY}
      ALGORITHM: ${ALGORITHM:-HS256}
      YANDEX_MAPS_API_KEY: ${YANDEX_MAPS_API_KEY}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOGSTASH_URL: ${LOGSTASH_URL:-http://logstash:5000}
      SERVICE_NAME: ${SERVICE_NAME:-places-service}
      USE_VAULT: ${USE_VAULT:-false}
      VAULT_ADDR: ${VAULT_ADDR:-http://vault:8200}
      VAULT_ROLE_ID: ${PLACES_VAULT_ROLE_ID:-}
      VAULT_SECRET_ID: ${PLACES_VAULT_SECRET_ID:-}
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      vault:
        condition: service_healthy
    deploy:
      resources: *backend-resources

  reports-service:
    build:
      context: ./services/reports-service
      dockerfile: Dockerfile
    ports:
      - "8003:8002"
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://postgres:postgres_password@postgres:5432/fishing_db}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      CLOUDINARY_CLOUD_NAME: ${CLOUDINARY_CLOUD_NAME}
      CLOUDINARY_API_KEY: ${CLOUDINARY_API_KEY}
      CLOUDINARY_API_SECRET: ${CLOUDINARY_API_SECRET}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOGSTASH_URL: ${LOGSTASH_URL:-http://logstash:5000}
      SERVICE_NAME: ${SERVICE_NAME:-reports-service}
      USE_VAULT: ${USE_VAULT:-false}
      VAULT_ADDR: ${VAULT_ADDR:-http://vault:8200}
      VAULT_ROLE_ID: ${REPORTS_VAULT_ROLE_ID:-}
      VAULT_SECRET_ID: ${REPORTS_VAULT_SECRET_ID:-}
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      vault:
        condition: service_healthy
    deploy:
      resources: *backend-resources

  booking-service:
    build:
      context: ./services/booking-service
      dockerfile: Dockerfile
    ports:
      - "8004:8003"
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://postgres:postgres_password@postgres:5432/fishing_db}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOGSTASH_URL: ${LOGSTASH_URL:-http://logstash:5000}
      SERVICE_NAME: ${SERVICE_NAME:-booking-service}
      USE_VAULT: ${USE_VAULT:-false}
      VAULT_ADDR: ${VAULT_ADDR:-http://vault:8200}
      VAULT_ROLE_ID: ${BOOKING_VAULT_ROLE_ID:-}
      VAULT_SECRET_ID: ${BOOKING_VAULT_SECRET_ID:-}
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      vault:
        condition: service_healthy
    deploy:
      resources: *backend-resources

  shop-service:
    build:
      context: ./services/shop-service
      dockerfile: Dockerfile
    ports:
      - "8005:8004"
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://postgres:postgres_password@postgres:5432/fishing_db}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOGSTASH_URL: ${LOGSTASH_URL:-http://logstash:5000}
      SERVICE_NAME: ${SERVICE_NAME:-shop-service}
      USE_VAULT: ${USE_VAULT:-false}
      VAULT_ADDR: ${VAULT_ADDR:-http://vault:8200}
      VAULT_ROLE_ID: ${SHOP_VAULT_ROLE_ID:-}
      VAULT_SECRET_ID: ${SHOP_VAULT_SECRET_ID:-}
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      vault:
        condition: service_healthy
    deploy:
      resources: *backend-resources

  forecast-service:
    build:
      context: ./services/forecast-service
      dockerfile: Dockerfile
    ports:
      - "8007:8000"
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://postgres:postgres_password@postgres:5432/fishing_db}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      OPENWEATHERMAP_API_KEY: ${OPENWEATHERMAP_API_KEY}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOGSTASH_URL: ${LOGSTASH_URL:-http://logstash:5000}
      SERVICE_NAME: ${SERVICE_NAME:-forecast-service}
      USE_VAULT: ${USE_VAULT:-false}
      VAULT_ADDR: ${VAULT_ADDR:-http://vault:8200}
      VAULT_ROLE_ID: ${FORECAST_VAULT_ROLE_ID:-}
      VAULT_SECRET_ID: ${FORECAST_VAULT_SECRET_ID:-}
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      vault:
        condition: service_healthy
    deploy:
      resources: *backend-resources

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        DOCKER_ENV: "true"
        NEXT_PUBLIC_YANDEX_MAPS_API_KEY: ${YANDEX_MAPS_API_KEY}
        NEXT_PUBLIC_API_URL: http://localhost:3000
        NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${STRIPE_PUBLISHABLE_KEY}
    ports:
      - "3000:3000"
    environment:
      DOCKER_ENV: 'true'
      AUTH_SERVICE_URL: http://auth-service:8000
      PLACES_SERVICE_URL: http://places-service:8001
      REPORTS_SERVICE_URL: http://reports-service:8002
      BOOKING_SERVICE_URL: http://booking-service:8003
      SHOP_SERVICE_URL: http://shop-service:8004
      EMAIL_SERVICE_URL: http://email-service:8005
      FORECAST_SERVICE_URL: http://forecast-service:8000
    networks:
      - fishing-network
    restart: unless-stopped
    logging: *logging
    depends_on:
      - auth-service
      - places-service
      - reports-service
      - booking-service
      - shop-service
      - email-service
      - forecast-service
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

networks:
  fishing-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  vault_data:
